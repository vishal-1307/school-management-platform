---
/**
 * Role dispatcher: figures out who the signed-in user is via the backend
 * (/api/auth/me is authoritative) and forwards them to their portal.
 */
export const prerender = false;

import PortalLayout from "../layouts/PortalLayout.astro";
import PortalDispatcher from "../components/portal/PortalDispatcher.tsx";
---

<PortalLayout title="Opening your portal…">
  <PortalDispatcher client:only="react" />
</PortalLayout>
