# Backend API base URL (Render service URL in production)
PUBLIC_API_URL=http://localhost:8000

# Demo auth (role picker instead of Clerk) — remove once Clerk is configured
PUBLIC_DEV_AUTH=true

# Clerk (after the Clerk app is created)
# PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxx
# CLERK_SECRET_KEY=sk_test_xxxxxxxx
